# Sanatan backend env — copy to .env and fill in. NEVER commit the real .env.
# (Rotate any key that has ever been pasted into chat.)
PORT=8787

# Anthropic (deity persona brain — Haiku)
ANTHROPIC_API_KEY=sk-ant-...

# Sarvam (STT / TTS / translate — Indian languages)
SARVAM_API_KEY=sk_...

# LiveKit (realtime voice transport)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=API...
LIVEKIT_API_SECRET=...
