# Copy to .env for local dev. In production these live in Render env vars (never commit real keys).
SARVAM_API_KEY=
ANTHROPIC_API_KEY=
# PORT is injected by Render. Only set it locally if 7860 is taken.
# PORT=7860
