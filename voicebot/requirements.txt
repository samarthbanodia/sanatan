# Sanatan voice bot — Pipecat cascaded pipeline (Sarvam STT + Anthropic Haiku + Sarvam TTS).
# After the first successful `pip install` + smoke test, PIN pipecat-ai to the exact
# version that worked (a few import paths move between releases).
pipecat-ai[anthropic,sarvam,silero,websocket]>=0.0.60
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
loguru>=0.7
