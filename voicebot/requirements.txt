# Sanatan voice bot — Pipecat cascaded pipeline (Sarvam STT + Anthropic Haiku + Sarvam TTS).
# Pinned to 1.4.0 — bot.py is written against that version's API (universal LLMContext,
# pipecat.transports.websocket.fastapi, Sarvam STT/TTS services). Bump deliberately.
pipecat-ai[anthropic,sarvam,silero,websocket]==1.4.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
loguru>=0.7
